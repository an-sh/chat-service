
div.chatUsers
  div.userEntry(ng-repeat='user in data.userlist | filter:name | orderBy ')
    span(ng-if='!chatService.isAdmin(data.room)') {{user}}
    a(ng-if='chatService.isAdmin(data.room)'
      href='#'
      ng-click='functions.ban(user)')
      | {{user}}
